# Day 1 學習心得

## 今日完成

- [O] 建置好 Node.js + TypeScript 環境
- [O] 安裝 `@langchain/openai`、`dotenv` 等依賴
- [O] 設定 `OPENAI_API_KEY`（.env）
- [O] 成功執行第一次 `ChatOpenAI.invoke()` 並取得回覆

## 筆記

### 學到的事

- **ChatOpenAI**：LangChain 用來呼叫 OpenAI 聊天模型的類別，建構時可傳 `model`、`temperature` 等。
- **invoke**：傳入字串或訊息陣列，會回傳 `AIMessage`，內容在 `response.content`。
- **環境變數**：用 `dotenv/config` 在程式開頭載入 `.env`，避免把 API Key 寫進程式碼。

### 遇到的問題

（寫下卡關的地方與解法，例如：API Key 沒讀到、網路錯誤、套件版本等）

- 問題：
- 解法：

### 參考資源

- [LangChain.js - ChatOpenAI](https://js.langchain.com/docs/integrations/chat/openai)
- 課綱：[CURRICULUM.md](../CURRICULUM.md) Day 1

## 明日預告（Day 2）

- Prompt 與 PromptTemplate：用模板組裝 prompt，變數替換、ChatPromptTemplate。

---

*填寫日期：_____*
